
import fc from "fast-check";
import add_segments from "../snake.js";

//describe("Check increase in length ", function () {

//     it("", function () {

//             const head = ___
//             if (answer !== reverse) {
//                 throw (
//                     `Expected "${string}" to reverse to "${answer}" ` +
//                     `insted we got "${reverse}"